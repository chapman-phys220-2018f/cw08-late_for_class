#!/usr/bin/env python3

def sinesum(t, n):

    def sinf(x):
        T = 2 * np.pi
        ones = np.ones_like(x)
        return((4/np.pi) * ((np.multiply((np.divide(1, 2*k-1)), np.sin(np.divide(2*(2*k-1)*pi*t, T)))) out = ones, where=x!=0))

    s = sinf(x)
    return (x, s)